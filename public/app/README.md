# KOVA App screenshots

Add the two phone screenshots in this folder with these exact names:

- `kova-coach.png` — the Coach / Today screen
- `kova-history.png` — the History screen

The App page already loads these files automatically.
