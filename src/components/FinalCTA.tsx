import { ArrowUpRight } from "lucide-react";
import { motion } from "framer-motion";
import { useNavigate } from "react-router";
import { SectionReveal } from "./KovaBackground";

export function FinalCTA() { const navigate = useNavigate(); return <section className="section-shell relative overflow-hidden px-6 py-32 sm:py-44"><motion.div animate={{ scale: [1, 1.08, 1], opacity: [0.35, 0.55, 0.35] }} transition={{ duration: 8, repeat: Infinity, ease: "easeInOut" }} className="pointer-events-none absolute left-1/2 top-1/2 size-[360px] -translate-x-1/2 -translate-y-1/2 rounded-full bg-white/[0.055] blur-[100px]" /><SectionReveal className="relative mx-auto max-w-3xl text-center"><p className="eyebrow">Your next workout starts here</p><h2 className="mt-6 font-serif text-[clamp(3.4rem,8vw,7rem)] leading-[0.88] tracking-[-0.07em] text-white">Your training <em className="text-white/55">evolves with you.</em></h2><p className="mt-8 text-sm text-white/42">Start with KOVA AI.</p><button type="button" onClick={() => navigate("/auth?returnTo=%2Fdashboard")} className="group mt-8 inline-flex h-12 items-center gap-3 rounded-full bg-white px-6 text-xs font-semibold uppercase tracking-[0.12em] text-black transition-transform hover:scale-[1.03]">Explore KOVA AI <ArrowUpRight size={15} strokeWidth={1.8} className="transition-transform group-hover:translate-x-0.5 group-hover:-translate-y-0.5" /></button></SectionReveal></section>; }
